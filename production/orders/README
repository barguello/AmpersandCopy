order_parser_and_saver.py: used as the controller for the order submit field.  It parses the
   Accountedge order report and saves the customer, order, and line items to the database.
   It also returns a status for the view to use so that the customer will know the status of the
   data import
